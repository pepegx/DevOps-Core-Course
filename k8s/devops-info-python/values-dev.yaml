replicaCount: 1

app:
  environment: dev

config:
  logLevel: DEBUG

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

service:
  type: NodePort
  # Keep dev deterministic, but avoid colliding with existing lab services in this cluster.
  nodePort: 30091

livenessProbe:
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  initialDelaySeconds: 3
  periodSeconds: 5
