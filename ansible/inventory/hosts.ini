# Static inventory for Lab 5.
# Replace with your real VM data from Lab 4 (cloud) or local VM fallback.

[webservers]
# Cloud VM example (Yandex/AWS/etc.)
# lab4-cloud ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

# Local VM fallback example (from Lab 4 local verification pattern)
# lab4-local ansible_host=127.0.0.1 ansible_port=2222 ansible_user=<local_user> ansible_ssh_private_key_file=../terraform/.keys/lab04_id_rsa

# Active local test target (works out of the box in this repo when the local
# Ubuntu Docker test container is running; replace with your real VM for Lab 5 submission)
lab05-ubuntu2404 ansible_connection=community.docker.docker ansible_user=root

# Placeholder VM entry (uncomment and replace for real VM usage)
# lab5-target ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[webservers:vars]
ansible_python_interpreter=/usr/bin/python3
